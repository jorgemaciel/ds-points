{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "ds-points",
            "database": "${workspaceFolder:ds-points}/data/database.db"
        }
    ]
}